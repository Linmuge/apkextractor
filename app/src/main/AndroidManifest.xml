<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="info.muge.apkextractor">
    <uses-permission
        android:name="android.permission.QUERY_ALL_PACKAGES"
        tools:ignore="QueryAllPackagesPermission" />
    <application
        android:name="info.muge.apkextractor.MyApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name="info.muge.apkextractor.activities.MainActivity"
            android:theme="@style/AppTheme.NoActionBar"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <action android:name="android.intent.action.VIEW"/>
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name="info.muge.apkextractor.activities.AppDetailActivity"
            android:label="Detail"
            android:theme="@style/AppTheme.NoActionBar"/>
        <activity android:name="info.muge.apkextractor.activities.SettingActivity"
            android:label="@string/activity_settings_title"
            android:theme="@style/AppTheme.NoActionBar"/>

        <activity android:name="info.muge.apkextractor.activities.PackageDetailActivity"
            android:label="@string/activity_package_detail"
            android:theme="@style/AppTheme.NoActionBar"
            android:exported="true">
            <intent-filter android:scheme="http"
                tools:ignore="AppLinkUrlError">
                <action android:name="android.intent.action.VIEW"/>
                <category android:name="android.intent.category.DEFAULT"/>
                <data android:mimeType="*/*"/>
            </intent-filter>
        </activity>

        <provider
            android:authorities="info.muge.apkextractor.FileProvider"
            android:name="androidx.core.content.FileProvider"
            android:grantUriPermissions="true"
            android:exported="false">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>
        <meta-data
            android:name="android.max_aspect"
            android:value="2.4" />
    </application>

</manifest>